import RacoGeganter from "../../components/RacoGeganter";
import { racoGeganterPages } from "../../data";

# Les disfresses

En construcció.

export default function Layout({children}) {
  return <RacoGeganter
    title={racoGeganterPages.find(page => page.route === "/les-disfresses").name}
    selectedRoute={racoGeganterPages.find(page => page.route === "/les-disfresses").route}>{children}</RacoGeganter>;
}
