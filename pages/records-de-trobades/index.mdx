import RacoGeganter from "../../components/RacoGeganter";
import { racoGeganterPages } from "../../data";

# Records de les trobades

En construcció.

export default function Layout({children}) {
  return <RacoGeganter
    title={racoGeganterPages.find(page => page.route === "/records-de-trobades").name}
    selectedRoute={racoGeganterPages.find(page => page.route === "/records-de-trobades").route}>{children}</RacoGeganter>;
}
