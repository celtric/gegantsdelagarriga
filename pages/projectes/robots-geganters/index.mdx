import MarkdownPage from "../../../components/MarkdownPage";

# [Projectes](/projectes) &rsaquo; Robots geganters

- micro:bit
- maqueen
- gegants de cartró
- vídeo
- música midi -> micro:bit
- codi

export default function Layout({children}) {
  return <MarkdownPage title="Robots geganters">{children}</MarkdownPage>;
}
