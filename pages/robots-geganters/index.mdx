import RacoGeganter from "../../components/RacoGeganter";
import { racoGeganterPages } from "../../data";

# Robots geganters

En construcció.

export default function Layout({children}) {
  return <RacoGeganter
    title={racoGeganterPages.find(page => page.route === "/robots-geganters").name}
    selectedRoute={racoGeganterPages.find(page => page.route === "/robots-geganters").route}>{children}</RacoGeganter>;
}
