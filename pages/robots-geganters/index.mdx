import RacoGeganter from "../../components/RacoGeganter";
import { racoGeganterPages } from "../../data";

# Els vestits

En construcció.

export default function Layout({children}) {
  return <RacoGeganter
    title={racoGeganterPages.find(page => page.route === "/els-vestits").name}
    selectedRoute={racoGeganterPages.find(page => page.route === "/els-vestits").route}>{children}</RacoGeganter>;
}
